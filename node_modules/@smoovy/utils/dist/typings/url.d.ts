export interface BrowserUrl {
    protocol: string;
    host: string;
    hostname: string;
    port: string;
    pathname: string;
    search: string;
    hash: string;
}
export declare function parseUrl(url: string): BrowserUrl;
export declare function serializeUrl(url: BrowserUrl): string;
