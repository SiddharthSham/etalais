export declare class Resolver<T = void, E = T> {
    promise: Promise<T>;
    private _resolve;
    private _reject;
    private _completed;
    constructor();
    resolve(value?: T): Promise<T>;
    reject(value?: E): Promise<T>;
    readonly completed: boolean;
}
