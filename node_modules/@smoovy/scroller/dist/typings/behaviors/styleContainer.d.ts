import { ScrollBehavior } from '../core';
interface Config {
    /**
     * The default styling of the container element
     * Default: {
     *   width: '100%',
     *   height: '100%',
     *   overflow: 'hidden'
     * }
     */
    defaults?: Partial<{
        [key: string]: string;
    }>;
}
declare const behavior: ScrollBehavior<Config>;
export { Config as StyleContainerConfig };
export default behavior;
